# .env.example

# --- Core Bot Credentials ---
NODE_ENV=development
TOKEN=your_discord_bot_token_here
CLIENT_ID=your_bot_client_id_here
GUILD_ID=your_development_server_id_here
JOHAN_USER_ID=the_specific_user_id_to_monitor

# --- Application Configuration ---
# (NEW) Path to the SQLite database file.
# Useful for running in different environments (e.g., /data/walpurgis.db in Docker)
DATABASE_PATH=walpurgis.db

# (NEW) Cooldown in hours for the automatic archiving feature.
# Avoids hardcoding values in the messageCreate event.
COOLDOWN_HOURS=12